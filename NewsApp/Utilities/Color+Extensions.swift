import SwiftUI

extension Color {
    static let appBlue = Color(red: 0.13, green: 0.47, blue: 0.96)
    static let appCardBackground = Color(uiColor: .systemBackground)
    static let appPillBackground = Color(uiColor: .secondarySystemBackground)
    static let appSecondaryText = Color(uiColor: .secondaryLabel)
    static let appBorder = Color(uiColor: .separator).opacity(0.3)
}
